# NNNN — Title

> Rule: rule.identifier this waiver covers, e.g. field.quality
> Scope: a file path this waiver covers, or `*` for every file the rule examines
> Reason: why this exception is reviewed and accepted, not left to drift
> Expires: YYYY-MM-DD (optional -- omitted means permanent/structural, not "nobody got to it")

## What this covers

The specific finding(s) this waiver suppresses, and why the underlying condition is acceptable
rather than a defect to fix.

## References

The finding this waives, and anything else worth pointing at.

> **Revision log**
>
> | Date | Change | Class |
> |---|---|---|
